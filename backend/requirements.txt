# ExcelGPT backend dependencies (pinned).
# See architecture/python-libraries.md for the role of each library.

# --- Web & API ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# --- Data intelligence & computation ---
pandas==2.2.2
numpy==1.26.4
scipy==1.13.1
statsmodels==0.14.2
scikit-learn==1.5.1

# --- Visualization & Excel generation ---
matplotlib==3.9.2
seaborn==0.13.2
openpyxl==3.1.5

# --- AI intent layer ---
cerebras-cloud-sdk==1.5.0
httpx==0.27.2  # pinned: cerebras-cloud-sdk 1.5.0 is incompatible with httpx>=0.28

# --- Configuration & validation ---
python-dotenv==1.0.1
pydantic==2.9.2

# --- Column intelligence (semantic labels & display names) ---
fuzzywuzzy>=0.18.0          # fuzzy column-name matching for semantic labels
python-Levenshtein>=0.21.0  # speeds up fuzzywuzzy
babel>=2.13.0               # locale-aware number formatting
